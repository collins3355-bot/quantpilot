"""quantpilot — a quantization autotuner for local models.

Give it a model, a quality budget, and the machine you're standing at;
it produces the smallest/fastest quantized artifact that stays inside
that budget, plus a benchmark report showing its work.
"""

__version__ = "0.5.1"
